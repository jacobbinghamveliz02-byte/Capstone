import { CommandClass } from "../lib/CommandClass.js";
import * as v from "@zwave-js/core/validation";
export function validateArgs_MultiCommandCCAPI_send() {
    return (__decoratedMethod, { kind }) => {
        if (kind === "method") {
            return function send(commands) {
                v.assert(v.array({ kind: "parameter", name: "commands" }, 'CommandClass', v.class({ kind: "item", name: "commands" }, "CommandClass", CommandClass))(commands));
                return __decoratedMethod.call(this, commands);
            };
        }
    };
}
//# sourceMappingURL=MultiCommandCC._validateArgs.js.map