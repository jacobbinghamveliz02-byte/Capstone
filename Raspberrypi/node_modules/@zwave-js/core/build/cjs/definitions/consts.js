"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var consts_exports = {};
__export(consts_exports, {
  HOMEID_BYTES: () => HOMEID_BYTES,
  MAX_NODES: () => MAX_NODES,
  MAX_NODES_LR: () => MAX_NODES_LR,
  MAX_TRANSPORT_SERVICE_SESSION_ID: () => MAX_TRANSPORT_SERVICE_SESSION_ID,
  NUM_LR_NODEMASK_SEGMENT_BYTES: () => NUM_LR_NODEMASK_SEGMENT_BYTES,
  NUM_LR_NODES_PER_SEGMENT: () => NUM_LR_NODES_PER_SEGMENT,
  NUM_NODEMASK_BYTES: () => NUM_NODEMASK_BYTES
});
module.exports = __toCommonJS(consts_exports);
const MAX_NODES = 232;
const MAX_NODES_LR = 4e3;
const NUM_NODEMASK_BYTES = MAX_NODES / 8;
const NUM_LR_NODES_PER_SEGMENT = 128;
const NUM_LR_NODEMASK_SEGMENT_BYTES = NUM_LR_NODES_PER_SEGMENT / 8;
const HOMEID_BYTES = 4;
const MAX_TRANSPORT_SERVICE_SESSION_ID = 15;
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  HOMEID_BYTES,
  MAX_NODES,
  MAX_NODES_LR,
  MAX_TRANSPORT_SERVICE_SESSION_ID,
  NUM_LR_NODEMASK_SEGMENT_BYTES,
  NUM_LR_NODES_PER_SEGMENT,
  NUM_NODEMASK_BYTES
});
//# sourceMappingURL=consts.js.map
