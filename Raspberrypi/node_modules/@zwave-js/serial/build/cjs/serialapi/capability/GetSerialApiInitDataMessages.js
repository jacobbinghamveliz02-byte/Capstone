"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var GetSerialApiInitDataMessages_exports = {};
__export(GetSerialApiInitDataMessages_exports, {
  GetSerialApiInitDataRequest: () => GetSerialApiInitDataRequest,
  GetSerialApiInitDataResponse: () => GetSerialApiInitDataResponse
});
module.exports = __toCommonJS(GetSerialApiInitDataMessages_exports);
var import_core = require("@zwave-js/core");
var import_serial = require("@zwave-js/serial");
var import_shared = require("@zwave-js/shared");
var __esDecorate = function(ctor, descriptorIn, decorators, contextIn, initializers, extraInitializers) {
  function accept(f) {
    if (f !== void 0 && typeof f !== "function") throw new TypeError("Function expected");
    return f;
  }
  __name(accept, "accept");
  var kind = contextIn.kind, key = kind === "getter" ? "get" : kind === "setter" ? "set" : "value";
  var target = !descriptorIn && ctor ? contextIn["static"] ? ctor : ctor.prototype : null;
  var descriptor = descriptorIn || (target ? Object.getOwnPropertyDescriptor(target, contextIn.name) : {});
  var _, done = false;
  for (var i = decorators.length - 1; i >= 0; i--) {
    var context = {};
    for (var p in contextIn) context[p] = p === "access" ? {} : contextIn[p];
    for (var p in contextIn.access) context.access[p] = contextIn.access[p];
    context.addInitializer = function(f) {
      if (done) throw new TypeError("Cannot add initializers after decoration has completed");
      extraInitializers.push(accept(f || null));
    };
    var result = (0, decorators[i])(kind === "accessor" ? { get: descriptor.get, set: descriptor.set } : descriptor[key], context);
    if (kind === "accessor") {
      if (result === void 0) continue;
      if (result === null || typeof result !== "object") throw new TypeError("Object expected");
      if (_ = accept(result.get)) descriptor.get = _;
      if (_ = accept(result.set)) descriptor.set = _;
      if (_ = accept(result.init)) initializers.unshift(_);
    } else if (_ = accept(result)) {
      if (kind === "field") initializers.unshift(_);
      else descriptor[key] = _;
    }
  }
  if (target) Object.defineProperty(target, contextIn.name, descriptor);
  done = true;
};
var __runInitializers = function(thisArg, initializers, value) {
  var useValue = arguments.length > 2;
  for (var i = 0; i < initializers.length; i++) {
    value = useValue ? initializers[i].call(thisArg, value) : initializers[i].call(thisArg);
  }
  return useValue ? value : void 0;
};
let GetSerialApiInitDataRequest = (() => {
  let _classDecorators = [(0, import_serial.messageTypes)(import_serial.MessageType.Request, import_serial.FunctionType.GetSerialApiInitData), (0, import_serial.expectedResponse)(import_serial.FunctionType.GetSerialApiInitData), (0, import_serial.priority)(import_core.MessagePriority.Controller)];
  let _classDescriptor;
  let _classExtraInitializers = [];
  let _classThis;
  let _classSuper = import_serial.Message;
  var GetSerialApiInitDataRequest2 = class extends _classSuper {
    static {
      __name(this, "GetSerialApiInitDataRequest");
    }
    static {
      _classThis = this;
    }
    static {
      const _metadata = typeof Symbol === "function" && Symbol.metadata ? Object.create(_classSuper[Symbol.metadata] ?? null) : void 0;
      __esDecorate(null, _classDescriptor = { value: _classThis }, _classDecorators, { kind: "class", name: _classThis.name, metadata: _metadata }, null, _classExtraInitializers);
      GetSerialApiInitDataRequest2 = _classThis = _classDescriptor.value;
      if (_metadata) Object.defineProperty(_classThis, Symbol.metadata, { enumerable: true, configurable: true, writable: true, value: _metadata });
      __runInitializers(_classThis, _classExtraInitializers);
    }
  };
  return GetSerialApiInitDataRequest2 = _classThis;
})();
let GetSerialApiInitDataResponse = (() => {
  let _classDecorators = [(0, import_serial.messageTypes)(import_serial.MessageType.Response, import_serial.FunctionType.GetSerialApiInitData)];
  let _classDescriptor;
  let _classExtraInitializers = [];
  let _classThis;
  let _classSuper = import_serial.Message;
  var GetSerialApiInitDataResponse2 = class extends _classSuper {
    static {
      __name(this, "GetSerialApiInitDataResponse");
    }
    static {
      _classThis = this;
    }
    static {
      const _metadata = typeof Symbol === "function" && Symbol.metadata ? Object.create(_classSuper[Symbol.metadata] ?? null) : void 0;
      __esDecorate(null, _classDescriptor = { value: _classThis }, _classDecorators, { kind: "class", name: _classThis.name, metadata: _metadata }, null, _classExtraInitializers);
      GetSerialApiInitDataResponse2 = _classThis = _classDescriptor.value;
      if (_metadata) Object.defineProperty(_classThis, Symbol.metadata, { enumerable: true, configurable: true, writable: true, value: _metadata });
      __runInitializers(_classThis, _classExtraInitializers);
    }
    constructor(options) {
      super(options);
      this.zwaveApiVersion = options.zwaveApiVersion;
      this.isPrimary = options.isPrimary;
      this.nodeType = options.nodeType;
      this.supportsTimers = options.supportsTimers;
      this.isSIS = options.isSIS;
      this.nodeIds = options.nodeIds;
      this.zwaveChipType = options.zwaveChipType;
    }
    static from(raw, _ctx) {
      const apiVersion = raw.payload[0];
      let zwaveApiVersion;
      if (apiVersion < 10) {
        zwaveApiVersion = {
          kind: "legacy",
          version: apiVersion
        };
      } else {
        zwaveApiVersion = {
          kind: "official",
          version: apiVersion - 9
        };
      }
      const capabilities = raw.payload[1];
      const nodeType = capabilities & 1 ? import_core.NodeType["End Node"] : import_core.NodeType.Controller;
      const supportsTimers = !!(capabilities & 2);
      const isPrimary = !(capabilities & 4);
      const isSIS = !!(capabilities & 8);
      let offset = 2;
      let nodeIds = [];
      if (raw.payload.length > offset) {
        const nodeListLength = raw.payload[offset];
        if (nodeListLength === import_core.NUM_NODEMASK_BYTES && raw.payload.length >= offset + 1 + nodeListLength) {
          const nodeBitMask = raw.payload.subarray(offset + 1, offset + 1 + nodeListLength);
          nodeIds = (0, import_core.parseNodeBitMask)(nodeBitMask);
        }
        offset += 1 + nodeListLength;
      }
      const chipType = raw.payload[offset];
      const chipVersion = raw.payload[offset + 1];
      let zwaveChipType;
      if (chipType != void 0 && chipVersion != void 0) {
        zwaveChipType = (0, import_core.getZWaveChipType)(chipType, chipVersion);
      }
      return new this({
        zwaveApiVersion,
        nodeType,
        supportsTimers,
        isPrimary,
        isSIS,
        nodeIds,
        zwaveChipType
      });
    }
    zwaveApiVersion;
    isPrimary;
    nodeType;
    supportsTimers;
    isSIS;
    nodeIds;
    zwaveChipType;
    serialize(ctx) {
      let chipType;
      if (typeof this.zwaveChipType === "string") {
        chipType = (0, import_core.getChipTypeAndVersion)(this.zwaveChipType);
      } else {
        chipType = this.zwaveChipType;
      }
      this.payload = new import_shared.Bytes(3 + import_core.NUM_NODEMASK_BYTES + (chipType ? 2 : 0));
      let capabilities = 0;
      if (this.supportsTimers)
        capabilities |= 2;
      if (this.isSIS)
        capabilities |= 8;
      if (this.zwaveApiVersion.kind === "legacy") {
        this.payload[0] = this.zwaveApiVersion.version;
        if (this.nodeType === import_core.NodeType["End Node"])
          capabilities |= 1;
        if (!this.isPrimary)
          capabilities |= 4;
      } else {
        this.payload[0] = this.zwaveApiVersion.version + 9;
        if (this.nodeType === import_core.NodeType.Controller)
          capabilities |= 1;
        if (this.isPrimary)
          capabilities |= 4;
      }
      this.payload[1] = capabilities;
      this.payload[2] = import_core.NUM_NODEMASK_BYTES;
      const nodeBitMask = (0, import_core.encodeBitMask)(this.nodeIds, import_core.MAX_NODES);
      this.payload.set(nodeBitMask, 3);
      if (chipType) {
        this.payload[3 + import_core.NUM_NODEMASK_BYTES] = chipType.type;
        this.payload[3 + import_core.NUM_NODEMASK_BYTES + 1] = chipType.version;
      }
      return super.serialize(ctx);
    }
  };
  return GetSerialApiInitDataResponse2 = _classThis;
})();
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  GetSerialApiInitDataRequest,
  GetSerialApiInitDataResponse
});
//# sourceMappingURL=GetSerialApiInitDataMessages.js.map
