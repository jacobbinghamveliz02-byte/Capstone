"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var NoiseDecryptTransform_exports = {};
__export(NoiseDecryptTransform_exports, {
  NoiseDecryptTransform: () => NoiseDecryptTransform
});
module.exports = __toCommonJS(NoiseDecryptTransform_exports);
var import_shared = require("@zwave-js/shared");
class NoiseDecryptTransformer {
  static {
    __name(this, "NoiseDecryptTransformer");
  }
  receiveCipher;
  constructor(receiveCipher) {
    this.receiveCipher = receiveCipher;
  }
  // transform() is called sequentially by the stream API,
  // which ensures nonce synchronization without needing a processingQueue
  async transform(chunk, controller) {
    const decrypted = await this.receiveCipher.decryptWithAd(new import_shared.Bytes(0), chunk);
    controller.enqueue(decrypted);
  }
}
class NoiseDecryptTransform extends TransformStream {
  static {
    __name(this, "NoiseDecryptTransform");
  }
  constructor(receiveCipher) {
    super(new NoiseDecryptTransformer(receiveCipher));
  }
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  NoiseDecryptTransform
});
//# sourceMappingURL=NoiseDecryptTransform.js.map
