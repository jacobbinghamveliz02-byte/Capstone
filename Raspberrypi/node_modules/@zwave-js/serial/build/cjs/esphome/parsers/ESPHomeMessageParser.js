"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var ESPHomeMessageParser_exports = {};
__export(ESPHomeMessageParser_exports, {
  ESPHomeMessageParser: () => ESPHomeMessageParser
});
module.exports = __toCommonJS(ESPHomeMessageParser_exports);
var import_core = require("@zwave-js/core");
var import_shared = require("@zwave-js/shared");
var import_ESPHomeMessage = require("../ESPHomeMessage.js");
class ESPHomeMessageParserTransformer {
  static {
    __name(this, "ESPHomeMessageParserTransformer");
  }
  receiveBuffer = new import_shared.Bytes();
  noiseMode;
  constructor(options = {}) {
    this.noiseMode = options.noiseMode ?? false;
  }
  transform(chunk, controller) {
    this.receiveBuffer = import_shared.Bytes.concat([this.receiveBuffer, chunk]);
    while (this.receiveBuffer.length > 0) {
      try {
        if (this.noiseMode) {
          this.parseNoiseMessage(controller);
        } else {
          this.parsePlaintextMessage(controller);
        }
      } catch (error) {
        if (error instanceof import_core.ZWaveError && error.code === import_core.ZWaveErrorCodes.PacketFormat_Truncated) {
          break;
        }
        this.receiveBuffer = new import_shared.Bytes();
        break;
      }
    }
  }
  /**
   * Parse plaintext ESPHome message format: [0x00][VarInt size][VarInt type][payload]
   */
  parsePlaintextMessage(controller) {
    const { message, bytesRead } = import_ESPHomeMessage.ESPHomeMessage.parse(this.receiveBuffer);
    this.receiveBuffer = this.receiveBuffer.subarray(bytesRead);
    controller.enqueue(message);
  }
  /**
   * Parse Noise-decrypted message format: [2-byte BE type][2-byte BE len][payload]
   */
  parseNoiseMessage(controller) {
    const { message, bytesRead } = import_ESPHomeMessage.ESPHomeMessage.parseFromNoise(this.receiveBuffer);
    this.receiveBuffer = this.receiveBuffer.subarray(bytesRead);
    controller.enqueue(message);
  }
}
class ESPHomeMessageParser extends TransformStream {
  static {
    __name(this, "ESPHomeMessageParser");
  }
  constructor(options = {}) {
    super(new ESPHomeMessageParserTransformer(options));
  }
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  ESPHomeMessageParser
});
//# sourceMappingURL=ESPHomeMessageParser.js.map
